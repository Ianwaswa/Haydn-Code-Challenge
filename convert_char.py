# This program uses the ord function to convert a character to its ASCII value.
char = "A"
code_point = ord(char)
print(code_point)
# Now use the chr function to convert the ASCII value back to a character.
print(chr(code_point))